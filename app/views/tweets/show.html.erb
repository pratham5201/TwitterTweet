<p style="color: green"><%= notice %></p>

<%= render @tweet %>

<div>
  <%= link_to "Edit this tweet", edit_tweet_path(@tweet) %> |
  <%= link_to "Back to tweets", tweets_path %>
  <%= button_to "delete", @tweet, data: { turbo_method: :delete, turbo_confirm: "Are you sure?" } %>
</div>
